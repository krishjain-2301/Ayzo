# AYZO dashboard

Next.js app for the AYZO local red-team platform. Setup, architecture, and API docs live in the [repository README](../../README.md).

```bash
# from the repo root
pnpm dev:web
```

The dashboard expects the API at `http://127.0.0.1:8000` (`NEXT_PUBLIC_API_URL`).
